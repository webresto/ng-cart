import { DishCalcProDirective } from './dish-calc-pro.directive';

describe('DishCalcProDirective', () => {
  it('should create an instance', () => {
    const directive = new DishCalcProDirective();
    expect(directive).toBeTruthy();
  });
});
