import { AmountCartDirective } from './amount-cart.directive';

describe('AmountCartDirective', () => {
  it('should create an instance', () => {
    const directive = new AmountCartDirective();
    expect(directive).toBeTruthy();
  });
});
